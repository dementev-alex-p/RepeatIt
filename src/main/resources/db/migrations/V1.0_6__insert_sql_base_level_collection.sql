SET search_path TO repeat_it;

INSERT INTO card_collection (name, author_id, is_public)
VALUES ('SQL. Базовый уровень', 1, true);

INSERT INTO card (user_id, card_collection_id, front_side, back_side)
SELECT
    1,
    (SELECT card_collection_id FROM card_collection WHERE name = 'SQL. Базовый уровень'),
    front_side,
    back_side
FROM (VALUES ('Что такое PRIMARY KEY?',
              'Первичный ключ - уникальный идентификатор строки в таблице. Гарантирует уникальность и не может содержать NULL. Пример: id SERIAL PRIMARY KEY. В PostgreSQL автоматически создает уникальный индекс.'),
             ('Для чего используется FOREIGN KEY?',
              'Внешний ключ обеспечивает ссылочную целостность между таблицами. Создает связь между таблицами через ссылку на PRIMARY KEY другой таблицы. Пример: user_id INTEGER REFERENCES users(id).'),
             ('Типы JOIN в SQL',
              'INNER JOIN - только совпадающие строки; LEFT JOIN - все строки левой таблицы; RIGHT JOIN - все строки правой; FULL JOIN - все строки обеих таблиц. CROSS JOIN - декартово произведение.'),
             ('Разница BETWEEN и IN',
              'BETWEEN проверяет диапазон значений: WHERE price BETWEEN 10 AND 100. IN проверяет принадлежность списку: WHERE status IN (''active'', ''pending''). BETWEEN включает границы.'),
             ('GROUP BY и HAVING',
              'GROUP BY группирует строки по столбцам для агрегатных функций. HAVING фильтрует результаты группировки (после GROUP BY), тогда как WHERE фильтрует до группировки.'),
             ('Агрегатные функции SQL',
              'COUNT() - подсчет строк; SUM() - сумма; AVG() - среднее; MAX()/MIN() - максимум/минимум. Используются с GROUP BY. Пример: SELECT department, AVG(salary) FROM employees GROUP BY department.'),
             ('Что такое NULL?',
              'Специальное значение, обозначающее отсутствие данных. Не равно нулю или пустой строке. Проверка: IS NULL или IS NOT NULL. Агрегатные функции игнорируют NULL.'),
             ('Типы данных в PostgreSQL',
              'Числовые: INTEGER, SERIAL, DECIMAL; Строковые: VARCHAR(n), TEXT; Дата/время: DATE, TIMESTAMP; Логический: BOOLEAN; Массивы: INTEGER[]. SERIAL - автоинкремент.'),
             ('Что такое индекс?',
              'Структура для ускорения поиска данных. Как индекс в книге - позволяет быстро найти данные без полного сканирования таблицы. В PostgreSQL основные типы: B-tree, Hash, GIN, GiST.'),
             ('Когда создавать индекс?',
              'На часто используемых столбцах в WHERE; на столбцах JOIN; на PRIMARY KEY и FOREIGN KEY. Не на маленьких таблицах или столбцах с малым количеством уникальных значений.'),
             ('B-tree индекс',
              'Самый распространенный тип индекса. Эффективен для точного поиска, диапазонов и сортировки. Балансированное дерево. В PostgreSQL индекс по умолчанию.'),
             ('Преимущества и недостатки индексов',
              'Плюсы: ускорение SELECT. Минусы: замедление INSERT/UPDATE/DELETE (нужно обновлять индекс); занимает место на диске. Создавать индексы обдуманно.'),
             ('Что такое транзакция?',
              'Последовательность операций, выполняемая как единое целое. Все операции выполняются или ни одна (при ошибке - откат). Начинается с BEGIN, заканчивается COMMIT или ROLLBACK.'),
             ('ACID свойства транзакций',
              'Atomicity - атомарность (всё или ничего); Consistency - согласованность; Isolation - изолированность; Durability - долговечность. Гарантируют надежность операций.'),
             ('Уровни изоляции в PostgreSQL',
              'Read Committed (по умолчанию) - видимы коммиты других транзакций; Repeatable Read - снимок на начало транзакции; Serializable - строгая изоляция.'),
             ('Что такое DEADLOCK?',
              'Взаимная блокировка, когда две транзакции ждут друг друга. Пример: T1 заблокировала A, ждет B; T2 заблокировала B, ждет A. PostgreSQL автоматически обнаруживает и отменяет одну.'),
             ('Команда EXPLAIN',
              'Показывает план выполнения запроса. Помогает понять, как СУБД выполняет запрос: использует ли индексы, какие JOIN методы. EXPLAIN ANALYZE - выполняет запрос и показывает фактические данные.'),
             ('Seq Scan vs Index Scan',
              'Seq Scan - последовательное чтение всей таблицы; Index Scan - чтение через индекс. На больших таблицах Index Scan обычно быстрее для поиска отдельных строк.'),
             ('Нормализация базы данных',
              'Процесс организации данных для уменьшения избыточности и аномалий. 1НФ - атомарные значения; 2НФ - зависит от всего ключа; 3НФ - зависит только от ключа.'),
             ('Первая нормальная форма (1НФ)',
              'Каждое поле содержит атомарные значения; нет повторяющихся групп. Все значения в столбце одного типа; нет массивов/списков в одной ячейке.'),
             ('Вторая нормальная форма (2НФ)',
              'Выполнена 1НФ; все неключевые атрибуты полностью зависят от всего первичного ключа (нет частичных зависимостей). Для таблиц с составным первичным ключом.'),
             ('Третья нормальная форма (3НФ)',
              'Выполнена 2НФ; нет транзитивных зависимостей (неключевые атрибуты не зависят от других неключевых атрибутов). Устраняет избыточность.'),
             ('Денормализация',
              'Сознательное нарушение нормальных форм для повышения производительности. Добавление избыточных данных для уменьшения JOIN. Используется в отчетах, аналитических системах.'),
             ('Типы отношений между таблицами',
              'Один-к-одному (one-to-one); один-ко-многим (one-to-many); многие-ко-многим (many-to-many). Many-to-many требует промежуточной таблицы.'),
             ('Что такое VIEW?',
              'Виртуальная таблица - сохраненный запрос SELECT. Упрощает сложные запросы, обеспечивает уровень абстракции. CREATE VIEW active_users AS SELECT * FROM users WHERE is_active = true.'),
             ('Чем VIEW отличается от таблицы?',
              'VIEW не хранит данные, а только определение запроса. Данные берутся из базовых таблиц при каждом обращении. Можно использовать как таблицу в SELECT.'),
             ('Команда CREATE TABLE',
              'Создание новой таблицы: CREATE TABLE users (id SERIAL PRIMARY KEY, name VARCHAR(50), email VARCHAR(100) UNIQUE, created_at TIMESTAMP DEFAULT now()).'),
             ('Ограничения (CONSTRAINTS) в таблицах',
              'PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, CHECK. Обеспечивают целостность данных. Пример: CHECK (age >= 0), UNIQUE (email).'),
             ('Команды INSERT, UPDATE, DELETE',
              'INSERT - добавление строк; UPDATE - изменение существующих; DELETE - удаление строк. Все могут использоваться с WHERE для выбора конкретных строк.'),
             ('WHERE и операторы сравнения',
              'Фильтрация строк: =, != или <>, >, <, >=, <=, BETWEEN, IN, LIKE (поиск по шаблону), IS NULL. Комбинируются с AND, OR, NOT.'),
             ('LIKE и ILIKE',
              'Поиск по шаблону в тексте: LIKE ''John%'' (начинается с John), LIKE ''%son'' (заканчивается на son). LIKE учитывает регистр, ILIKE - нет (только в PostgreSQL).'),
             ('ORDER BY и сортировка',
              'Сортировка результатов: ORDER BY column1 ASC, column2 DESC. ASC - по возрастанию (по умолчанию), DESC - по убыванию. Можно сортировать по нескольким столбцам.'),
             ('LIMIT и OFFSET',
              'LIMIT ограничивает количество возвращаемых строк; OFFSET пропускает указанное количество строк. Используются для пагинации: LIMIT 10 OFFSET 20 (строки 21-30).'),
             ('Подзапросы (subqueries)',
              'Запрос внутри другого запроса. Может быть в SELECT, FROM, WHERE. Пример: SELECT name FROM products WHERE price > (SELECT AVG(price) FROM products).'),
             ('UNION и UNION ALL',
              'Объединение результатов двух запросов. UNION удаляет дубликаты, UNION ALL оставляет все строки. Оба запроса должны иметь одинаковые столбцы.'),
             ('Работа с датами и временем',
              'Типы: DATE, TIME, TIMESTAMP. Функции: NOW(), CURRENT_DATE, EXTRACT(year FROM date_column), DATE_PART. Интервалы: INTERVAL ''1 day''.'),
             ('Функции для работы со строками',
              'LENGTH(), UPPER()/LOWER(), TRIM(), SUBSTRING(), CONCAT(), REPLACE(). Пример: SELECT CONCAT(first_name, '' '', last_name) AS full_name FROM users.'),
             ('Что такое VACUUM в PostgreSQL?',
              'Очистка "мертвых" строк (удаленных или обновленных). Освобождает место для повторного использования. AUTOVACUUM запускается автоматически. Важно для производительности.'),
             ('Резервное копирование pg_dump',
              'Утилита для логического бэкапа: pg_dump dbname > backup.sql. Восстановление: psql dbname < backup.sql. Сохраняет схему и/или данные.'),
             ('Пользователи и привилегии',
              'CREATE USER name WITH PASSWORD ''pass''; GRANT SELECT ON table TO user; REVOKE DELETE ON table FROM user. Управление доступом к объектам БД.'),
             ('Типы репликации',
              'Master-Slave: master для записи, slave для чтения. Репликация обеспечивает отказоустойчивость и распределение нагрузки. В PostgreSQL - streaming replication.'),
             ('Что такое WAL?',
              'Write-Ahead Log - журнал предзаписи. Все изменения сначала пишутся в WAL, затем в основные данные. Обеспечивает надежность и используется для репликации.'),
             ('Мониторинг производительности',
              'pg_stat_activity - текущие сессии; pg_stat_all_tables - статистика по таблицам; pg_stat_statements - статистика запросов. Выявляют медленные запросы.'),
             ('Поиск медленных запросов',
              'Включить log_min_duration_statement в postgresql.conf; использовать pg_stat_statements; анализировать EXPLAIN ANALYZE проблемных запросов.'),
             ('Оптимизация запросов',
              'Использовать индексы; избегать SELECT *; минимизировать подзапросы; использовать JOIN вместо подзапросов; ограничивать выборку LIMIT.'),
             ('Что такое connection pool?',
              'Пул соединений - кэш открытых соединений с БД. Уменьшает накладные расходы на установление соединения. Примеры: PgBouncer, встроенный в приложения.'),
             ('Транзакции в приложении',
              'Начинать транзакцию перед группой операций; делать транзакции короткими; обрабатывать ошибки (откат); не оставлять открытые транзакции.'),
             ('Миграции базы данных',
              'Управление изменениями схемы БД: создание/изменение таблиц, индексов. Инструменты: Flyway, Liquibase. Версионирование изменений.'),
             ('Типы блокировок',
              'Row-level locks - блокировки строк; Table-level locks - блокировки таблиц. Автоматически управляются СУБД, но можно использовать SELECT FOR UPDATE.'),
             ('JSONB в PostgreSQL',
              'Бинарное хранение JSON данных. Поддерживает индексацию, эффективный поиск. Операторы: @> (содержит), ? (существует ключ). Пример: WHERE data @> ''{"status": "active"}''.'),
             ('Массивы в PostgreSQL',
              'Тип данных для хранения массивов: INTEGER[], TEXT[]. Операторы: && (пересечение), @> (содержит). Функции: array_length(), unnest(). Уникальная особенность PostgreSQL.'),
             ('Полнотекстовый поиск',
              'Поиск по тексту с учетом морфологии. Типы: tsvector (нормализованный текст), tsquery (поисковый запрос). Индексы GIN для ускорения.'),
             ('Расширения PostgreSQL',
              'Дополнительные модули: PostGIS (геоданные), pg_trgm (похожий поиск), uuid-ossp (генерация UUID). Устанавливаются: CREATE EXTENSION extension_name.'),
             ('Команда ALTER TABLE',
              'Изменение структуры таблицы: ADD COLUMN, DROP COLUMN, ALTER COLUMN, ADD CONSTRAINT. Пример: ALTER TABLE users ADD COLUMN phone VARCHAR(15).')) AS cards(front_side, back_side);