telegram:
  bot:
    name : RepeatIt_tgbot
    token: ${TG_BOT_TOKEN}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/repeat_it_db
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        default_schema: repeat_it

  flyway:
    enabled: true
    locations: classpath:db/migrations
    schemas: repeat_it
    baseline-on-migrate: true
    validate-on-migrate: true
    clean-disabled: true
    table: flyway_schema_history